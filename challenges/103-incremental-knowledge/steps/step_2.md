# Step 2: Second Requirement — Rate Limiting

A new requirement has arrived for the server configuration.

Read your previous requirements from `setup/constraints.json`.

**Requirement 2:** The server must have a **rate limiter** set to **100 requests per minute per IP**, with a **burst allowance of 20**.

Add this requirement to `setup/constraints.json` alongside the existing one(s). Do not overwrite the previous requirements.
