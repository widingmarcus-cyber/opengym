# Step 3: Third Requirement — Logging

A new requirement has arrived for the server configuration.

Read your previous requirements from `setup/constraints.json`.

**Requirement 3:** The server must log to **both stdout and a file**:
- **stdout**: log level **INFO**
- **file**: path **/var/log/app.log**, log level **DEBUG**

Add this requirement to `setup/constraints.json` alongside the existing ones. Do not overwrite the previous requirements.
