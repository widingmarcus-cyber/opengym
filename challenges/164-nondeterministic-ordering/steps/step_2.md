You are the Verifier agent. Your task is to check whether the results are properly sorted.

1. Read `setup/results.json`.
2. Check if the entries are sorted by `id` in ascending order (1, 2, 3, 4, 5, 6).
3. Write "SORTED" to `setup/answer.txt` if they are sorted, or "UNSORTED" if they are not.

Expected answer.txt content:
```
SORTED
```
