# Step 1: Agent A Writes

You are acting as **Agent A**.

Write Agent A's data to `setup/output.json` under the key `"agent_a"`:

```json
{
  "agent_a": {
    "agent_a_data": [1, 2, 3]
  }
}
```

The file must be valid JSON after this step.
