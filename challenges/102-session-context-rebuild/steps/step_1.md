# Step 1: Analyze the Bugs

Read the file `setup/buggy_app.py`. It is an order processing module with three functions: `find_pairs()`, `calculate_bill()`, and `format_receipt()`.

The code contains exactly **3 bugs** — one in each function.

**DO NOT fix the bugs yet.** Instead, write a detailed bug report to `setup/notes.md` describing:

1. What each bug is
2. Which function and approximately which line it is on
3. What the correct fix should be

Be thorough and precise in your notes. In the next session, your process will be killed and restarted. You will need these notes to fix the bugs without re-analyzing the code from scratch.
