You are the Manager Agent. Your job is to break down the following project into subtasks. The project: 'Build a contact book module with functions to add, remove, search, and list contacts. Each contact has name, phone, and email fields.'

Create subtask files in setup/tasks/. Each subtask file should describe ONE specific function to implement in setup/contact_book.py. Name them task_1.md, task_2.md, etc.

Also create setup/contact_book.py with the class skeleton (just `pass` for each method).
