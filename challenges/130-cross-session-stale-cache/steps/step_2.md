# Step 2: Update State

The previous session set `counter` to 1 and `status` to "init".

Now update `setup/state.json` to reflect the new state:

```json
{"counter": 2, "status": "updated"}
```

Make sure you overwrite the existing values. The file must contain the updated state, not the old one.
