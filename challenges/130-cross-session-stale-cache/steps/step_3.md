# Step 3: Read Current State

Note: In a previous session, the counter was set to 1. However, the state may have been updated since then.

Read `setup/state.json` and write the **current** value of `counter` to `setup/answer.txt`.

Write only the number, nothing else. Do not rely on any remembered or cached values -- always read directly from the file.
