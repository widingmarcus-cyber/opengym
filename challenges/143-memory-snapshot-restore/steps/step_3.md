# Step 3: Report the Theme

Read `setup/state.json` and write the value of `config.theme` to `setup/answer.txt`.

The value should be just the theme string (e.g., `light`), with no additional text or formatting.
