# Step 2: Report Migration Count

Read `setup/data.json` and count how many records were successfully migrated (i.e., how many records exist in the file).

Write the count as a plain number to `setup/answer.txt`.

For example, if there are 2 migrated records, write `2`.
