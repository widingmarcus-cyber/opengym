You are the Counter agent. Your task is to count the unique events after deduplication.

1. Read `setup/processed.json` (deduplicated events from Step 1).
2. Count the number of unique events in the array.
3. Write the count as a string to `setup/answer.txt`.

Expected answer.txt content:
```
5
```
