You are Agent A. Your task is to write the first line to the shared log file.

Append the following line to `setup/log.txt`:

```
A:step1
```

The file should contain exactly one line after this step. Make sure to include a newline at the end of the line.
