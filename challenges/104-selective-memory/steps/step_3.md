# Step 3: Recall from Memory

The original `setup/records.txt` has been deleted.

Using ONLY your stored data in `setup/memory.json`, write `setup/answers.json` containing a list of the important projects.

Format:
```json
{
  "important_projects": [
    {"name": "Project X", "budget": 12345, "status": "active"},
    ...
  ]
}
```

Include all important projects you stored. The budget should be a number (no dollar sign or commas).
