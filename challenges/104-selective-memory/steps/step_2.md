# Step 2: Distraction Task

Write a Python function in `setup/distraction.py` that implements binary search on a sorted list.

The function signature should be:

```python
def binary_search(arr, target) -> int:
```

It should return the index of the target in the sorted list, or -1 if not found.
