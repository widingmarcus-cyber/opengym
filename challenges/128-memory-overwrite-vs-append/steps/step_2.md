# Step 2: Overwrite the Value

The key `"color"` currently has the value `"blue"` in `setup/memory.json`.

Update `"color"` to `"red"`.

**Important:** You must OVERWRITE the existing key, not append a second entry. The resulting JSON must have exactly one `"color"` key with the value `"red"`.
