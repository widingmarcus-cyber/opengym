# Step 1: Store Initial Value

Open `setup/memory.json` and store the key `"color"` with the value `"blue"`.

The file should be valid JSON after this step. For example:

```json
{"color": "blue"}
```
