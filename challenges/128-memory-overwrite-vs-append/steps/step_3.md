# Step 3: Recall the Value

Read `setup/memory.json` and determine the current value of the key `"color"`.

Write that value (and nothing else) to `setup/answer.txt`.

The file should contain only the value string, with no quotes, no extra whitespace, and no newline beyond the value itself.
