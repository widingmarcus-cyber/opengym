# Step 2: Distraction Task

Solve this unrelated coding task:

Write a Python function in `setup/distraction.py` called `sum_primes_below_100()` that returns the sum of all prime numbers less than 100.

The function should:
- Take no arguments
- Return an integer (the sum of all primes below 100)
- Be correct

This is a standalone task. Complete it fully before moving on.
