# Step 1: Initial Implementation

Implement the function `process_data(records)` in `setup/solution.py`.

It should:
1. Group records by category
2. Calculate the average value per category
3. Return a dict of `{category: average}`

Read `setup/requirements.md` for full details.

Write your implementation and note any assumptions you made in `setup/attempt_log.json`.
