def process_data(records):
    """Group records by category and calculate average value per category.

    Args:
        records: list of dicts with 'category' and 'value' keys

    Returns:
        dict mapping category to average value
    """
    pass  # Implement this
