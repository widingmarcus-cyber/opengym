# Step 2: Agent A Writes

You are Agent A. Your task is to update the server port configuration.

Read `setup/state.json` and update the `port` field under `config` to `8080`. Write the updated state back to `setup/state.json`.

Keep all other fields (like `host`) unchanged.
