# Step 3: Coordinator Aggregates

You are acting as the **Coordinator**.

Read `setup/shared.json`, which contains results from both Agent A and Agent B.

1. Extract the `result` value from each agent entry
2. Compute the total by summing all agent results

Write the aggregated result to `setup/answer.json` with a single key `"total"` containing the computed sum.
