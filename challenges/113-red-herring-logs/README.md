# Challenge 113: Red Herring Logs

The application in `setup/` is producing wrong results. A log file at `setup/app.log` shows the application's recent output. The config is in `setup/config.yaml`. The main code is in `setup/processor.py`.

Find and fix the issue so all tests pass.
