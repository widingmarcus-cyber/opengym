# Step 2: Report Record Count

Read `setup/memory.json` and count the number of records stored in the file.

Write the count as a plain number to `setup/answer.txt`.

For example, if there are 10 records, write `10`.
