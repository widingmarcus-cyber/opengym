You are the Extractor agent. Your task is to extract ordered data from the sorted events.

1. Read `setup/ordered.json` (sorted events from Step 1).
2. Extract the `data` value from each event in order.
3. Join them as a comma-separated string (no spaces after commas).
4. Write the result to `setup/answer.txt`.

Expected answer.txt content:
```
alpha,beta,gamma,delta,epsilon
```
