# Step 3: Report Result

Read `setup/memory.json` and count how many keys remain after compaction.

Write that count (as a plain integer string) to `setup/answer.txt`.

The file should contain only the number, with no quotes, no extra whitespace, and no trailing newline beyond the value itself.
