# Step 2: Retrieve Config at Version 2

Your task: retrieve the value of `"config"` at **version 2** from `setup/memory.json`.

Write the value (and nothing else) to `setup/answer.txt`.

The file should contain only the value string, with no quotes, no extra whitespace, and no trailing newline.
