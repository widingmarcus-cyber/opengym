# Step 3: Report the Version

Read `setup/store.json` and write the value of the `version` field to `setup/answer.txt`.

The value should be just the number (e.g., `2`), with no additional text or formatting.
